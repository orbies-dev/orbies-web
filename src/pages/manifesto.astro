---
import Layout from "../layouts/Layout.astro";
import Icon from "../components/Icon.astro";
---

<Layout
  title="Manifesto - Orbs"
  description="Small, focused apps built rapidly with AI. One problem. One solution. Open source."
>
  <!-- Hero Section -->
  <section class="manifesto-hero">
    <div class="hero-background">
      <div class="warp-grid"></div>
      <div class="floating-shapes">
        <div class="shape shape-1"></div>
        <div class="shape shape-2"></div>
        <div class="shape shape-3"></div>
      </div>
    </div>

    <div class="container">
      <div class="hero-header">
        <div class="hero-badge fade-in-up">
          <div class="badge-icon">
            <Icon name="note" size={16} />
          </div>
          <span>The manifesto</span>
        </div>
        <h1 class="hero-title fade-in-up">
          Build small.<br />
          Share freely.<br />
          <span class="hero-accent">Move fast.</span>
        </h1>
        <p class="hero-subtitle fade-in-up">Solve one problem this weekend</p>
      </div>
    </div>
  </section>

  <!-- Main Content -->
  <article class="manifesto-content section-padding">
    <div class="container">
      <!-- The Idea -->
      <section class="idea-section glass-card fade-in-up">
        <h2>The idea</h2>
        <p class="intro-text">An Orb is a tiny packaged application that:</p>
        <div class="features-grid">
          <div class="feature-item">
            <div class="feature-icon">
              <Icon name="target" size={20} />
            </div>
            <span>Solves one specific problem</span>
          </div>
          <div class="feature-item">
            <div class="feature-icon">
              <Icon name="clock" size={20} />
            </div>
            <span>Takes hours (not weeks) to build</span>
          </div>
          <div class="feature-item">
            <div class="feature-icon">
              <Icon name="terminal" size={20} />
            </div>
            <span>Ships as a ready-to-use binary</span>
          </div>
          <div class="feature-item">
            <div class="feature-icon">
              <Icon name="github" size={20} />
            </div>
            <span>Lives on GitHub for anyone to download</span>
          </div>
          <div class="feature-item">
            <div class="feature-icon">
              <Icon name="star" size={20} />
            </div>
            <span>Gets built with AI providing support</span>
          </div>
        </div>
        <p class="highlight">
          <Icon name="orb" size={16} />
          Think of them as micro-products—small enough to build quickly, useful enough
          to share.
        </p>
      </section>

      <!-- Why Orbs -->
      <section class="why-section glass-card fade-in-up">
        <h2>Why "Orbs"</h2>
        <p>
          The name suggests something small, self-contained, and complete. Like
          a marble or a sphere—simple geometry, no unnecessary complexity. You
          can hold an Orb in your hand (or download it in seconds).
        </p>
      </section>

      <!-- The Process -->
      <section class="process-section fade-in-up">
        <div class="section-icon-center">
          <Icon name="code" size={32} />
        </div>
        <h2 class="section-title">The process</h2>
        <div class="process-grid">
          <div class="process-card glass-card">
            <div class="process-icon-small">
              <Icon name="alert" size={20} />
            </div>
            <h3>Identify a pain point</h3>
            <p>Something annoying in your daily workflow</p>
          </div>
          <div class="process-card glass-card">
            <div class="process-icon-small">
              <Icon name="target" size={20} />
            </div>
            <h3>Design minimal solution</h3>
            <p>What's the smallest thing that would help?</p>
          </div>
          <div class="process-card glass-card">
            <div class="process-icon-small">
              <Icon name="star" size={20} />
            </div>
            <h3>Build with AI</h3>
            <p>Leverage AI for unfamiliar tech stacks</p>
          </div>
          <div class="process-card glass-card">
            <div class="process-icon-small">
              <Icon name="external" size={20} />
            </div>
            <h3>Ship it</h3>
            <p>Package, release, move on</p>
          </div>
        </div>
        <p class="process-note">
          <Icon name="clock" size={16} />
          The async nature of working with AI means you can start an Orb, let AI
          work through the details, and come back to something functional.
        </p>
      </section>

      <!-- Philosophy -->
      <section class="philosophy-section fade-in-up">
        <h2 class="section-title">Philosophy</h2>
        <p class="section-intro">Orbs embrace:</p>
        <div class="philosophy-grid">
          <div class="philosophy-card glass-card">
            <div class="philosophy-icon">
              <Icon name="lightning" size={24} />
            </div>
            <h3>Speed over perfection</h3>
            <p>Ship it, iterate if needed</p>
          </div>
          <div class="philosophy-card glass-card">
            <div class="philosophy-icon">
              <Icon name="target" size={24} />
            </div>
            <h3>Focus over features</h3>
            <p>One job, done well</p>
          </div>
          <div class="philosophy-card glass-card">
            <div class="philosophy-icon">
              <Icon name="users" size={24} />
            </div>
            <h3>Sharing over hoarding</h3>
            <p>If it helped you, share it</p>
          </div>
          <div class="philosophy-card glass-card">
            <div class="philosophy-icon">
              <Icon name="code" size={24} />
            </div>
            <h3>Learning over expertise</h3>
            <p>Build in languages you don't know yet</p>
          </div>
        </div>
      </section>

      <!-- Anti-patterns -->
      <section class="anti-section glass-card fade-in-up">
        <div class="section-icon">
          <Icon name="x" size={32} />
        </div>
        <h2>Anti-patterns</h2>
        <p class="intro-text">Orbs are NOT:</p>
        <div class="anti-grid">
          <div class="anti-item">
            <Icon name="x" size={20} />
            <span>Large, complex applications</span>
          </div>
          <div class="anti-item">
            <Icon name="x" size={20} />
            <span>Commercial products (they're free)</span>
          </div>
          <div class="anti-item">
            <Icon name="x" size={20} />
            <span>Platforms or frameworks</span>
          </div>
          <div class="anti-item">
            <Icon name="x" size={20} />
            <span>Anything requiring ongoing maintenance</span>
          </div>
        </div>
        <p class="highlight">
          <Icon name="alert" size={16} />
          If it needs a roadmap, it's not an Orb.
        </p>
      </section>

      <!-- CTA -->
      <section class="cta-section glass-card-strong fade-in-up">
        <div class="cta-icon">
          <Icon name="orb" size={48} />
        </div>
        <h2>Building your own Orbs</h2>
        <p>
          The barrier is low. Got an idea? Spend an evening with AI. See what
          happens. Maybe you'll ship something useful. Maybe you'll learn a new
          stack. Either way, you win.
        </p>
        <div class="cta-stats">
          <div class="stat-item">
            <Icon name="orb" size={20} />
            <span>300+ Orbs</span>
          </div>
          <div class="stat-item">
            <Icon name="clock" size={20} />
            <span>2.1 hours average</span>
          </div>
          <div class="stat-item">
            <Icon name="github" size={20} />
            <span>100% open source</span>
          </div>
        </div>
        <div
          style="display: flex; gap: var(--space-4); justify-content: center; flex-wrap: wrap;"
        >
          <a href="/apps" class="liquid-btn">
            <Icon name="orb" size={20} />
            <span>Explore Orbs</span>
            <Icon name="arrow-right" size={16} />
          </a>
          <a
            href="/publish"
            class="liquid-btn"
            style="background: var(--accent); border-color: var(--accent);"
          >
            <Icon name="star" size={20} />
            <span>Share your Orb</span>
          </a>
        </div>
      </section>
    </div>
  </article>
</Layout>

<style>
  /* ============================================
     MANIFESTO PAGE - VISUAL & CLEAN
     ============================================ */

  /* ============================================
     HERO SECTION
     ============================================ */
  .manifesto-hero {
    min-height: 60vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: calc(var(--header-height) + 3rem) 0 3rem;
    position: relative;
    overflow: hidden;
    background: var(--bg-primary);
  }

  .hero-background {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
    opacity: 0.6;
  }

  .warp-grid {
    position: absolute;
    inset: 0;
    background-image: linear-gradient(
        to right,
        var(--border-primary) 1px,
        transparent 1px
      ),
      linear-gradient(to bottom, var(--border-primary) 1px, transparent 1px);
    background-size: 60px 60px;
    animation: gridFloat 20s ease-in-out infinite;
  }

  .floating-shapes {
    position: absolute;
    inset: 0;
  }

  .shape-1,
  .shape-2,
  .shape-3 {
    position: absolute;
    border-radius: 50%;
    background: radial-gradient(circle, var(--primary-glow), transparent);
    animation: floatShape 15s ease-in-out infinite;
  }

  .shape-1 {
    width: 300px;
    height: 300px;
    top: 10%;
    left: 10%;
    animation-delay: 0s;
  }

  .shape-2 {
    width: 200px;
    height: 200px;
    bottom: 20%;
    right: 15%;
    animation-delay: 5s;
  }

  .shape-3 {
    width: 150px;
    height: 150px;
    top: 50%;
    right: 30%;
    animation-delay: 10s;
  }

  @keyframes gridFloat {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-20px);
    }
  }

  @keyframes floatShape {
    0%,
    100% {
      transform: translate(0, 0) scale(1);
      opacity: 0.3;
    }
    50% {
      transform: translate(30px, -30px) scale(1.1);
      opacity: 0.5;
    }
  }

  .hero-header {
    text-align: center;
    position: relative;
    z-index: 10;
    max-width: 900px;
    margin: 0 auto;
  }

  .hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1.5rem;
    background: var(--glass-primary);
    backdrop-filter: var(--glass-blur);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-full);
    font-size: var(--text-sm);
    font-weight: var(--font-semibold);
    color: var(--text-secondary);
    margin-bottom: 2rem;
    transition: all var(--transition-normal);
  }

  .hero-badge:hover {
    transform: translateY(-2px);
    border-color: var(--border-glass);
    box-shadow: var(--shadow-md);
  }

  .badge-dot {
    width: 8px;
    height: 8px;
    background: var(--primary);
    border-radius: 50%;
    animation: pulse 2s ease-in-out infinite;
  }

  @keyframes pulse {
    0%,
    100% {
      opacity: 1;
      transform: scale(1);
    }
    50% {
      opacity: 0.6;
      transform: scale(1.3);
    }
  }

  .hero-title {
    font-size: clamp(3rem, 8vw, 5.5rem);
    font-weight: var(--font-black);
    line-height: 1.1;
    letter-spacing: -0.04em;
    color: var(--text-primary);
    margin: 0 0 1.5rem 0;
  }

  .hero-accent {
    background: var(--gradient-primary);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .hero-subtitle {
    font-size: clamp(1.125rem, 2.5vw, 1.5rem);
    color: var(--text-secondary);
    font-weight: var(--font-medium);
    max-width: 600px;
    margin: 0 auto;
  }

  /* ============================================
     MAIN CONTENT
     ============================================ */
  .manifesto-content {
    background: var(--bg-secondary);
    position: relative;
  }

  .section-padding {
    padding: var(--space-24) 0;
  }

  .section-title {
    font-size: clamp(var(--font-size-2xl), 4vw, var(--font-size-4xl));
    font-weight: var(--font-weight-bold);
    color: var(--text-primary);
    margin: 0 0 var(--space-6) 0;
    letter-spacing: -0.02em;
    text-align: center;
  }

  .section-intro {
    font-size: var(--font-size-lg);
    color: var(--text-secondary);
    text-align: center;
    margin: 0 0 var(--space-12) 0;
  }

  /* Glass Card Base */
  .glass-card {
    padding: var(--space-10);
    background: var(--bg-glass);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-2xl);
    backdrop-filter: blur(var(--blur-md));
    -webkit-backdrop-filter: blur(var(--blur-md));
    margin-bottom: var(--space-12);
    transition: all var(--transition-normal);
    position: relative;
    overflow: hidden;
  }

  .glass-card::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--accent-soft);
    opacity: 0;
    transition: opacity var(--transition-normal);
    pointer-events: none;
  }

  .glass-card:hover::before {
    opacity: 1;
  }

  .glass-card:hover {
    transform: translateY(-4px);
    border-color: var(--border-strong);
    box-shadow: var(--shadow-xl);
  }

  .glass-card-strong {
    padding: var(--space-16);
    background: var(--bg-glass-strong);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-3xl);
    backdrop-filter: blur(var(--blur-lg));
    -webkit-backdrop-filter: blur(var(--blur-lg));
    text-align: center;
    box-shadow: var(--shadow-2xl);
    margin-bottom: 0;
    position: relative;
    overflow: hidden;
  }

  .glass-card-strong::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: var(--accent-soft);
    opacity: 0.3;
    pointer-events: none;
  }

  /* Typography */
  .glass-card h2,
  .glass-card-strong h2 {
    font-size: clamp(var(--font-size-xl), 3vw, var(--font-size-3xl));
    font-weight: var(--font-weight-bold);
    color: var(--text-primary);
    margin: 0 0 var(--space-6) 0;
    letter-spacing: -0.02em;
    position: relative;
    z-index: 1;
  }

  .intro-text {
    font-size: var(--font-size-lg);
    color: var(--text-secondary);
    margin: 0 0 var(--space-8) 0;
    line-height: 1.6;
    position: relative;
    z-index: 1;
  }

  .highlight {
    font-size: var(--font-size-lg);
    color: var(--text-primary);
    font-weight: var(--font-weight-semibold);
    font-style: italic;
    background: var(--accent-soft);
    padding: var(--space-6);
    border-radius: var(--radius-lg);
    border-left: 3px solid var(--accent);
    margin: var(--space-8) 0 0 0;
    position: relative;
    z-index: 1;
    display: flex;
    align-items: center;
    gap: var(--space-3);
  }

  /* Section Icons */
  .section-icon {
    width: 64px;
    height: 64px;
    margin: 0 auto var(--space-6);
    border-radius: var(--radius-xl);
    background: var(--accent);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-lg);
    position: relative;
    z-index: 1;
  }

  .section-icon-center {
    width: 56px;
    height: 56px;
    margin: 0 auto var(--space-8);
    border-radius: var(--radius-xl);
    background: var(--accent);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-lg);
  }

  .badge-icon {
    color: var(--accent);
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* The Idea Section */
  .features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-6);
    margin: var(--space-8) 0;
    position: relative;
    z-index: 1;
  }

  .feature-item {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-4);
    background: var(--bg-glass);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-lg);
    backdrop-filter: blur(var(--blur-sm));
    -webkit-backdrop-filter: blur(var(--blur-sm));
    transition: all var(--transition-normal);
  }

  .feature-item:hover {
    transform: translateX(4px);
    border-color: var(--border-strong);
    background: var(--bg-glass-strong);
  }

  .feature-icon {
    width: 40px;
    height: 40px;
    border-radius: var(--radius-lg);
    background: var(--accent);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    box-shadow: var(--shadow-md);
    transition: all var(--transition-normal);
  }

  .feature-item:hover .feature-icon {
    transform: scale(1.1);
    box-shadow: var(--shadow-glow);
  }

  .feature-item span {
    color: var(--text-secondary);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-medium);
  }

  /* Process Section */
  .process-section {
    margin-bottom: var(--space-12);
  }

  .process-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
    gap: var(--space-6);
    margin-top: var(--space-8);
  }

  .process-card {
    padding: var(--space-8);
    position: relative;
  }

  .process-number {
    width: 48px;
    height: 48px;
    border-radius: 50%;
    background: var(--accent);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: var(--font-size-xl);
    font-weight: var(--font-weight-bold);
    margin-bottom: var(--space-4);
    box-shadow: var(--shadow-lg);
    transition: all var(--transition-normal);
  }

  .process-card:hover .process-number {
    transform: scale(1.1);
    box-shadow: var(--shadow-glow);
  }

  .process-icon-small {
    width: 36px;
    height: 36px;
    border-radius: var(--radius-lg);
    background: var(--bg-glass);
    border: 1px solid var(--border-primary);
    color: var(--accent);
    display: flex;
    align-items: center;
    justify-content: center;
    margin-bottom: var(--space-3);
    transition: all var(--transition-normal);
    position: relative;
    z-index: 1;
  }

  .process-card:hover .process-icon-small {
    background: var(--accent-soft);
    border-color: var(--accent);
  }

  .process-card h3 {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--text-primary);
    margin: 0 0 var(--space-3) 0;
    position: relative;
    z-index: 1;
  }

  .process-card p {
    color: var(--text-secondary);
    line-height: 1.6;
    margin: 0;
    font-size: var(--font-size-base);
    position: relative;
    z-index: 1;
  }

  .process-note {
    text-align: center;
    color: var(--text-tertiary);
    font-size: var(--font-size-base);
    font-style: italic;
    margin: var(--space-10) 0 0 0;
    padding: var(--space-6);
    background: var(--bg-glass);
    border-radius: var(--radius-lg);
    border: 1px solid var(--border-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    gap: var(--space-3);
  }

  /* Example Section */
  .example-item {
    display: flex;
    align-items: center;
    gap: var(--space-6);
    padding: var(--space-6);
    background: var(--bg-glass);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-xl);
    backdrop-filter: blur(var(--blur-sm));
    -webkit-backdrop-filter: blur(var(--blur-sm));
    transition: all var(--transition-normal);
    position: relative;
    z-index: 1;
  }

  .example-item:hover {
    transform: translateX(4px);
    border-color: var(--border-strong);
    background: var(--bg-glass-strong);
    box-shadow: var(--shadow-lg);
  }

  .example-icon {
    width: 56px;
    height: 56px;
    border-radius: var(--radius-xl);
    background: var(--accent);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    box-shadow: var(--shadow-md);
    transition: all var(--transition-normal);
  }

  .example-item:hover .example-icon {
    transform: scale(1.05);
    box-shadow: var(--shadow-glow);
  }

  .example-content {
    flex: 1;
  }

  .example-content h3 {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--text-primary);
    margin: 0 0 var(--space-2) 0;
  }

  .example-content p {
    color: var(--text-secondary);
    margin: 0;
    font-size: var(--font-size-base);
  }

  .example-link {
    width: 40px;
    height: 40px;
    border-radius: var(--radius-lg);
    background: var(--bg-glass);
    border: 1px solid var(--border-primary);
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--text-tertiary);
    transition: all var(--transition-normal);
    flex-shrink: 0;
  }

  .example-item:hover .example-link {
    background: var(--accent);
    color: white;
    border-color: var(--accent);
    transform: translateX(4px);
  }

  /* Philosophy Section */
  .philosophy-section {
    margin-bottom: var(--space-12);
  }

  .philosophy-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(260px, 1fr));
    gap: var(--space-6);
  }

  .philosophy-card {
    padding: var(--space-8);
    text-align: center;
  }

  .philosophy-icon {
    width: 56px;
    height: 56px;
    margin: 0 auto var(--space-4);
    border-radius: var(--radius-xl);
    background: var(--accent);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-lg);
    transition: all var(--transition-normal);
    position: relative;
    z-index: 1;
  }

  .philosophy-card:hover .philosophy-icon {
    transform: scale(1.1) translateY(-4px);
    box-shadow: var(--shadow-glow);
  }

  .philosophy-card h3 {
    font-size: var(--font-size-lg);
    font-weight: var(--font-weight-semibold);
    color: var(--text-primary);
    margin: 0 0 var(--space-3) 0;
    position: relative;
    z-index: 1;
  }

  .philosophy-card p {
    color: var(--text-secondary);
    line-height: 1.5;
    margin: 0;
    font-size: var(--font-size-base);
    position: relative;
    z-index: 1;
  }

  /* Anti-patterns Section */
  .anti-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
    gap: var(--space-4);
    margin: var(--space-8) 0;
    position: relative;
    z-index: 1;
  }

  .anti-item {
    display: flex;
    align-items: center;
    gap: var(--space-4);
    padding: var(--space-4);
    background: var(--bg-glass);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-lg);
    backdrop-filter: blur(var(--blur-sm));
    -webkit-backdrop-filter: blur(var(--blur-sm));
    transition: all var(--transition-normal);
    color: var(--text-tertiary);
  }

  .anti-item:hover {
    border-color: var(--red);
    background: rgba(239, 68, 68, 0.05);
  }

  /* CTA Section */
  .cta-icon {
    width: 80px;
    height: 80px;
    margin: 0 auto var(--space-6);
    border-radius: var(--radius-2xl);
    background: var(--accent);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    box-shadow: var(--shadow-xl), var(--shadow-glow);
    transition: all var(--transition-normal);
    position: relative;
    z-index: 1;
  }

  .glass-card-strong:hover .cta-icon {
    transform: scale(1.1) rotate(5deg);
  }

  .cta-section p {
    color: var(--text-secondary);
    font-size: var(--font-size-lg);
    line-height: 1.6;
    margin: 0 0 var(--space-8) 0;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    position: relative;
    z-index: 1;
  }

  .cta-stats {
    display: flex;
    justify-content: center;
    gap: var(--space-8);
    margin: var(--space-8) 0;
    flex-wrap: wrap;
    position: relative;
    z-index: 1;
  }

  .stat-item {
    display: flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-4) var(--space-6);
    background: var(--bg-glass);
    border: 1px solid var(--border-primary);
    border-radius: var(--radius-xl);
    backdrop-filter: blur(var(--blur-sm));
    -webkit-backdrop-filter: blur(var(--blur-sm));
    color: var(--text-secondary);
    font-size: var(--font-size-sm);
    font-weight: var(--font-weight-medium);
    transition: all var(--transition-normal);
  }

  .stat-item:hover {
    background: var(--accent-soft);
    border-color: var(--accent);
    color: var(--text-primary);
    transform: translateY(-2px);
  }

  .liquid-btn {
    display: inline-flex;
    align-items: center;
    gap: var(--space-3);
    padding: var(--space-4) var(--space-8);
    background: var(--black);
    color: white;
    border: 1px solid var(--black);
    border-radius: var(--radius-xl);
    font-size: var(--font-size-base);
    font-weight: var(--font-weight-semibold);
    text-decoration: none;
    transition: all var(--transition-normal);
    box-shadow: var(--shadow-md);
    position: relative;
    z-index: 1;
    overflow: hidden;
  }

  .liquid-btn::before {
    content: "";
    position: absolute;
    top: 0;
    left: -100%;
    width: 100%;
    height: 100%;
    background: linear-gradient(
      90deg,
      transparent,
      rgba(255, 255, 255, 0.2),
      transparent
    );
    transition: left 0.6s ease;
  }

  .liquid-btn:hover::before {
    left: 100%;
  }

  .liquid-btn:hover {
    transform: translateY(-2px);
    box-shadow:
      var(--shadow-xl),
      0 0 20px var(--red-glow);
    background: var(--black-soft);
  }

  /* ============================================
     ANIMATIONS
     ============================================ */
  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  .fade-in-up {
    animation: fadeInUp 0.8s ease-out both;
  }

  /* ============================================
     RESPONSIVE DESIGN
     ============================================ */
  @media (max-width: 768px) {
    .section-padding {
      padding: var(--space-16) 0;
    }

    .manifesto-hero {
      min-height: 50vh;
      padding: calc(var(--header-height) + 2rem) 0 2rem;
    }

    .hero-title {
      font-size: clamp(2rem, 10vw, 3rem);
    }

    .glass-card {
      padding: var(--space-8);
      margin-bottom: var(--space-8);
    }

    .glass-card-strong {
      padding: var(--space-10);
    }

    .features-grid,
    .process-grid,
    .philosophy-grid,
    .anti-grid {
      grid-template-columns: 1fr;
      gap: var(--space-4);
    }

    .warp-grid {
      background-size: 50px 50px;
    }

    .shape {
      filter: blur(40px);
      opacity: 0.3;
    }
  }

  @media (max-width: 480px) {
    .glass-card {
      padding: var(--space-6);
    }

    .glass-card-strong {
      padding: var(--space-8);
      border-radius: var(--radius-2xl);
    }

    .example-item {
      flex-direction: column;
      text-align: center;
    }

    .example-link {
      align-self: stretch;
    }

    .cta-stats {
      flex-direction: column;
      gap: var(--space-4);
    }

    .stat-item {
      width: 100%;
      justify-content: center;
    }

    .process-note {
      flex-direction: column;
    }
  }

  /* ============================================
     ACCESSIBILITY
     ============================================ */
  @media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
      animation: none !important;
      transition: none !important;
    }

    .glass-card:hover,
    .process-card:hover,
    .philosophy-card:hover,
    .feature-item:hover,
    .example-item:hover,
    .liquid-btn:hover {
      transform: none !important;
    }
  }

  /* Focus states */
  .liquid-btn:focus-visible,
  .example-link:focus-visible {
    outline: 2px solid var(--accent);
    outline-offset: 2px;
  }

  /* ============================================
     ICON MICROINTERACTIONS
     ============================================ */

  /* Badge icon - gentle pulse */
  .badge-icon {
    animation: gentlePulse 3s ease-in-out infinite;
  }

  @keyframes gentlePulse {
    0%,
    100% {
      transform: scale(1);
      opacity: 1;
    }
    50% {
      transform: scale(1.05);
      opacity: 0.8;
    }
  }

  /* Section icons - rotate on hover */
  .section-icon:hover {
    transform: rotate(10deg) scale(1.05);
  }

  .section-icon-center:hover {
    transform: rotate(-10deg) scale(1.05);
  }

  /* Feature icons - bounce slightly on hover */
  .feature-icon {
    transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
  }

  /* Process icon small - wiggle */
  .process-card:hover .process-icon-small {
    animation: wiggle 0.6s ease-in-out;
  }

  @keyframes wiggle {
    0%,
    100% {
      transform: rotate(0deg);
    }
    25% {
      transform: rotate(-5deg);
    }
    75% {
      transform: rotate(5deg);
    }
  }

  /* Example icon - gentle float */
  .example-icon {
    transition: all 0.4s ease;
  }

  .example-item:hover .example-icon {
    animation: gentleFloat 2s ease-in-out infinite;
  }

  @keyframes gentleFloat {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-4px);
    }
  }

  /* Philosophy icons - pop */
  .philosophy-card:hover .philosophy-icon {
    animation: popIcon 0.5s ease-out;
  }

  @keyframes popIcon {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.2) rotate(5deg);
    }
    100% {
      transform: scale(1.1) translateY(-4px);
    }
  }

  /* Anti-pattern icons - shake warning */
  .anti-item:hover :global(svg) {
    animation: shakeWarning 0.5s ease;
    color: var(--red);
  }

  @keyframes shakeWarning {
    0%,
    100% {
      transform: translateX(0);
    }
    25% {
      transform: translateX(-3px);
    }
    75% {
      transform: translateX(3px);
    }
  }

  /* CTA icon - continuous rotate */
  .cta-icon {
    animation: gentleRotate 10s linear infinite;
  }

  @keyframes gentleRotate {
    from {
      transform: rotate(0deg);
    }
    to {
      transform: rotate(360deg);
    }
  }

  .glass-card-strong:hover .cta-icon {
    animation: bounceSpin 0.8s ease-out;
  }

  @keyframes bounceSpin {
    0% {
      transform: scale(1) rotate(0deg);
    }
    50% {
      transform: scale(1.2) rotate(180deg);
    }
    100% {
      transform: scale(1.1) rotate(360deg);
    }
  }

  /* Stat item icons - scale up */
  .stat-item:hover :global(svg) {
    animation: scaleUp 0.3s ease;
  }

  @keyframes scaleUp {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.3);
    }
    100% {
      transform: scale(1.15);
    }
  }

  /* Liquid button icons - slide */
  .liquid-btn:hover :global(svg) {
    animation: slideIcon 0.4s ease;
  }

  @keyframes slideIcon {
    0% {
      transform: translateX(0);
    }
    50% {
      transform: translateX(4px);
    }
    100% {
      transform: translateX(2px);
    }
  }

  /* Highlight icons - glow */
  .highlight :global(svg) {
    animation: glowPulse 2s ease-in-out infinite;
  }

  @keyframes glowPulse {
    0%,
    100% {
      filter: drop-shadow(0 0 0 var(--accent));
    }
    50% {
      filter: drop-shadow(0 0 4px var(--accent));
    }
  }

  /* Process note icon - tick */
  .process-note:hover :global(svg) {
    animation: tick 0.6s ease;
  }

  @keyframes tick {
    0%,
    100% {
      transform: rotate(0deg);
    }
    25% {
      transform: rotate(-15deg);
    }
    50% {
      transform: rotate(15deg);
    }
    75% {
      transform: rotate(-10deg);
    }
  }

  /* Example link arrow - slide right */
  .example-link:hover :global(svg) {
    animation: slideRight 0.4s ease infinite alternate;
  }

  @keyframes slideRight {
    from {
      transform: translateX(0);
    }
    to {
      transform: translateX(4px);
    }
  }
</style>
